void deleteNode(int positionOfNode)
{
    if (head==NULL)
    {
        cout<<"List is empty!"<<endl;
        return;
    }
linklist* node=head;
if(positionOfNode==1)
{
    head=node->next;
    node->next=NULL;
    delete node;
}
if (positionOfNode>1)
{
    for (int j=1; j<positionOfNode; j++)
    {


        if (j==positionOfNode-1)
        {
            linklist* newNode;
            newNode=node->next;
            node->next=NULL;
            node->next=newNode->next;
            newNode->next=NULL;
            delete newNode;
        }

        node=node->next;
    }
}

}